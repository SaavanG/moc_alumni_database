{
  "name": "moc-alumni-database",
  "version": "1.0.0",
  "description": "MOC Alumni Database - Connecting current members with alumni",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["alumni", "database", "moc", "networking"],
  "author": "MOC President",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 